/**
 * @ttu/ui — Legacy HeroUI wrappers.
 *
 * Note: most primitive wrappers have been moved to `@ttu/design-system`.
 * This package is kept for backward compatibility and for components that
 * still wrap HeroUI directly (which has its own theming layer).
 *
 * New code should prefer `@ttu/design-system`.
 */
export * from "./components";
