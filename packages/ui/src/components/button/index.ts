/**
 * Re-exports from `@ttu/design-system` for backward compatibility.
 * Existing code that imports from `@ttu/ui/button` keeps working.
 */
export { Button, buttonClasses, type ButtonProps } from "@ttu/design-system";
